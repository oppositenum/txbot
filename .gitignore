# 依赖 / 系统
node_modules/
.DS_Store

# SEA 打包产物(CI构建，本地不需要提交)
dist/
*.blob

# 账号数据（Cookie、账号密码明文、自动备份）—— 绝不提交
data/
cookies.json
*.cookies.json

# 浏览器登录态缓存
.chrome-profile/
data/browser-profiles/

# 探索/调试脚本与抓包存档（含硬编码测试账号密码，仅本地探索用，不进仓库）
probe/

# 日志
*.log
npm-debug.log*

# 密钥/环境变量（以后如有）
.env
.env.*
*.pem
*.key

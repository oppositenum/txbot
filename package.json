{
  "name": "txbot",
  "version": "1.0.0",
  "description": "天下农场多账号协议托管 - 跨平台 (Linux/Windows/macOS)",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "build:sea": "esbuild src/server.js --bundle --platform=node --outfile=dist/bundle.cjs --format=cjs"
  },
  "keywords": [
    "tx.com.cn",
    "farm",
    "bot"
  ],
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "undici": "^8.10.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright-core": "^1.63.0"
  }
}
